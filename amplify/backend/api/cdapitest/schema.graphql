type Class @model {
  id: ID!
  name: String!
  registrations: RegistrationConnection
}

type Registration @model {
  id: ID!
  name: String
  major: String
}

type RegistrationConnection @model {
  items: [Registration!]
  nextToken: String
}
